{
  "name": "content-build",
  "version": "1.0.0",
  "description": "Build pipeline for learning app content (YAML -> JSON)",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "deploy": "node src/build.js && firebase deploy --only hosting && node src/bump-version.js",
    "bump-version": "node src/bump-version.js",
    "validate": "node src/validate.js"
  },
  "dependencies": {
    "glob": "^10.3.10",
    "uuid": "^9.0.0",
    "yaml": "^2.3.4"
  }
}
