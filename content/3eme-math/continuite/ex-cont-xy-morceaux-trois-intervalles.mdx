---
type: exercise
title: Continuité par morceaux - trois intervalles
difficulty: 2
timeMinutes: 20
category: approfondissement
tags:
  - continuite
  - xy-plus
  - morceaux
  - droite-gauche
  - racine
source: XY Plus T1, Exercice 2, p.18
---

:::enonce
Soit $f$ la fonction définie par :

$$f(x) = \begin{cases} x - 1 & \text{si } x \leq 0 \\ 2x - 4 & \text{si } 0 < x < 3 \\ \sqrt{x - 1} & \text{si } x \geq 3 \end{cases}$$

1. Tracer la courbe représentative de $f$ dans un repère $(O, \vec{i}, \vec{j})$.
2. a. Justifier que la fonction $f$ est continue sur $]-\infty, 0]$.
   b. Justifier que la fonction $f$ est continue sur $]0, 3[$.
   c. Justifier que la fonction $f$ est continue sur $[3, +\infty[$.
3. Justifier à l'aide du graphique que la fonction $f$ n'est pas continue sur $\mathbb{R}$.
:::

:::solution
1. La restriction de $f$ à $]-\infty, 0]$ est la demi-droite passant par $A(0, -1)$ et $B(-2, -3)$.

La restriction de $f$ à $]0, 3[$ est le segment ouvert $]C, D[$ avec $C(0, -4)$ et $D(3, 2)$.

La restriction de $f$ à $[3, +\infty[$ est la courbe $\mathcal{C}_1$ de $x \mapsto \sqrt{x - 1}$.

```typst
#import "@preview/cetz:0.3.4": canvas, draw

#canvas({
  import draw: *

  // Axes
  line((-3.5, 0), (7, 0), stroke: 0.8pt, mark: (end: ">", fill: black))
  line((0, -5), (0, 3.5), stroke: 0.8pt, mark: (end: ">", fill: black))
  content((6.8, -0.3), $x$)
  content((-0.3, 3.3), $y$)
  content((-0.3, -0.3), $O$)

  // Ticks x
  for x in (-3, -2, -1, 1, 2, 3, 4, 5, 6) {
    line((x, -0.1), (x, 0.1), stroke: 0.5pt)
    content((x, -0.4), text(size: 7pt, str(x)))
  }

  // Ticks y
  for y in (-4, -3, -2, -1, 1, 2, 3) {
    line((-0.1, y), (0.1, y), stroke: 0.5pt)
    content((-0.4, y), text(size: 7pt, str(y)))
  }

  // Piece 1: y = x - 1 for x <= 0
  line((-3, -4), (0, -1), stroke: 1.5pt + blue)
  circle((0, -1), radius: 0.07, fill: blue, stroke: 1pt + blue)

  // Piece 2: y = 2x - 4 for 0 < x < 3
  line((0, -4), (3, 2), stroke: 1.5pt + blue)
  circle((0, -4), radius: 0.07, fill: white, stroke: 1pt + blue)
  circle((3, 2), radius: 0.07, fill: white, stroke: 1pt + blue)

  // Piece 3: y = sqrt(x - 1) for x >= 3
  circle((3, 1.41), radius: 0.07, fill: blue, stroke: 1pt + blue)
  bezier((3, 1.41), (6, 2.24), (4.2, 1.85), (5.2, 2.1), stroke: 1.5pt + blue)

  // Dashed lines showing discontinuities
  line((0, -1), (0, -4), stroke: (dash: "dashed", paint: gray))
  line((3, 1.41), (3, 2), stroke: (dash: "dashed", paint: gray))

  // Labels
  content((-2.5, -2.3), text(size: 8pt, fill: blue, $y = x - 1$))
  content((1.5, -0.5), text(size: 8pt, fill: blue, $y = 2x - 4$))
  content((5.5, 1), text(size: 8pt, fill: blue, $y = sqrt(x - 1)$))

  // Key points
  content((0.3, -0.7), text(size: 7pt, $A$))
  content((0.3, -4.3), text(size: 7pt, $C$))
  content((3.3, 2.3), text(size: 7pt, $D$))
})
```

2. a. La fonction $x \mapsto x - 1$ est un polynôme, donc continue sur $\mathbb{R}$, en particulier sur $]-\infty, 0]$. D'où $f$ est continue sur $]-\infty, 0]$.

b. La fonction $x \mapsto 2x - 4$ est un polynôme, donc continue sur $\mathbb{R}$, en particulier sur $]0, 3[$. D'où $f$ est continue sur $]0, 3[$.

c. La fonction $x \mapsto \sqrt{x - 1}$ est continue sur $[1, +\infty[$ et $[3, +\infty[ \subset [1, +\infty[$. D'où $f$ est continue sur $[3, +\infty[$.

3. La représentation graphique met en évidence un saut du tracé à droite du point $A(0, f(0))$.

En effet : $f(0) = 0 - 1 = -1$ et $\lim_{x \to 0^+} f(x) = \lim_{x \to 0^+} (2x - 4) = -4 \neq f(0)$.

Donc $f$ n'est pas continue en $0$, et $f$ n'est pas continue sur $\mathbb{R}$.
:::

:::methode
Pour une fonction définie par morceaux :

1. Étudier la continuité sur chaque morceau séparément (fonctions usuelles)
2. Vérifier la continuité aux points de raccordement en comparant $f(a)$, $\lim_{x \to a^-} f(x)$ et $\lim_{x \to a^+} f(x)$
:::
