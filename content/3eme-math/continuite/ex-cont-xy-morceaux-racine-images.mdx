---
type: exercise
title: Fonction par morceaux avec racine et images d'intervalles
difficulty: 2
timeMinutes: 25
category: approfondissement
tags:
  - continuite
  - xy-plus
  - morceaux
  - racine
  - image-intervalle
source: XY Plus T1, Exercice 6, p.19
---

:::enonce
Soit $f$ la fonction définie sur $\mathbb{R}$ par :

$$f(x) = \begin{cases} -x + 2 & \text{si } x \leq -1 \\ x^2 - 2x & \text{si } -1 < x < 2 \\ \sqrt{x + 2} & \text{si } x \geq 2 \end{cases}$$

1. Montrer que $f$ est continue sur chacun des intervalles : $]-\infty, -1]$, $]-1, 2[$ et $[2, +\infty[$.
2. a. Tracer $\mathcal{C}_f$ la courbe de $f$ dans un repère orthonormé $(O, \vec{i}, \vec{j})$.
   b. Justifier graphiquement que $f$ n'est pas continue sur $\mathbb{R}$.
   c. Déterminer graphiquement les images par $f$ des intervalles $[-4, -2]$ et $]-1, 2[$.
:::

:::solution
1. $x \mapsto -x + 2$ est une fonction affine donc continue sur $\mathbb{R}$, en particulier continue sur $]-\infty, -1]$.

$x \mapsto x^2 - 2x$ est une fonction polynôme donc continue sur $\mathbb{R}$, en particulier continue sur $]-1, 2[$.

$x \mapsto \sqrt{x + 2}$ est continue sur $[-2, +\infty[$ et $[2, +\infty[ \subset [-2, +\infty[$, donc $f$ est continue sur $[2, +\infty[$.

2. a.

```typst
#import "@preview/cetz:0.3.4": canvas, draw

#canvas({
  import draw: *

  // Axes
  line((-5, 0), (7, 0), stroke: 0.8pt, mark: (end: ">", fill: black))
  line((0, -1.8), (0, 7), stroke: 0.8pt, mark: (end: ">", fill: black))
  content((6.8, -0.3), $x$)
  content((-0.3, 6.8), $y$)
  content((-0.3, -0.3), $O$)

  // Ticks x
  for x in (-4, -3, -2, -1, 1, 2, 3, 4, 5, 6) {
    line((x, -0.1), (x, 0.1), stroke: 0.5pt)
    content((x, -0.4), text(size: 7pt, str(x)))
  }

  // Ticks y
  for y in (-1, 1, 2, 3, 4, 5, 6) {
    line((-0.1, y), (0.1, y), stroke: 0.5pt)
    content((-0.4, y), text(size: 7pt, str(y)))
  }

  // Piece 1: y = -x + 2 for x <= -1
  line((-4.5, 6.5), (-1, 3), stroke: 1.5pt + blue)
  circle((-1, 3), radius: 0.07, fill: blue, stroke: 1pt + blue)

  // Piece 2: y = x^2 - 2x for -1 < x < 2
  // (x-1)^2 - 1, vertex at (1, -1)
  bezier((-1, 3), (0, 0), (-0.6, 1.5), (-0.2, 0.3), stroke: 1.5pt + blue)
  bezier((0, 0), (1, -1), (0.4, -0.65), (0.7, -0.95), stroke: 1.5pt + blue)
  bezier((1, -1), (2, 0), (1.3, -0.95), (1.7, -0.5), stroke: 1.5pt + blue)
  circle((-1, 3), radius: 0.07, fill: white, stroke: 1pt + blue)
  circle((2, 0), radius: 0.07, fill: white, stroke: 1pt + blue)

  // Piece 3: y = sqrt(x + 2) for x >= 2
  circle((2, 2), radius: 0.07, fill: blue, stroke: 1pt + blue)
  bezier((2, 2), (6, 2.83), (3.5, 2.5), (5, 2.72), stroke: 1.5pt + blue)

  // Dashed line showing discontinuity at x = 2
  line((2, 0), (2, 2), stroke: (dash: "dashed", paint: gray))

  // Labels
  content((-3.5, 5), text(size: 8pt, fill: blue, $y = -x + 2$))
  content((0.5, -1.5), text(size: 8pt, fill: blue, $y = x^2 - 2x$))
  content((5, 1.5), text(size: 8pt, fill: blue, $y = sqrt(x + 2)$))
})
```

b. $\mathcal{C}_f$ présente une rupture au niveau du point d'abscisse $2$.

En effet : $\lim_{x \to 2^-} f(x) = \lim_{x \to 2^-} (x^2 - 2x) = 0$ et $f(2) = \sqrt{4} = 2$. Comme $0 \neq 2$, $f$ n'est pas continue en $2$, donc $f$ n'est pas continue sur $\mathbb{R}$.

c. $f([-4, -2])$ : sur $[-4, -2] \subset ]-\infty, -1]$, $f(x) = -x + 2$ est décroissante. $f(-4) = 6$, $f(-2) = 4$. Donc $f([-4, -2]) = [4, 6]$.

$f(]-1, 2[)$ : sur $]-1, 2[$, $f(x) = x^2 - 2x = (x - 1)^2 - 1$.

$f$ est décroissante sur $]-1, 1]$ puis croissante sur $[1, 2[$. Le minimum est $f(1) = -1$ et $\lim_{x \to -1^+} f(x) = 3$ (non atteint). Donc $f(]-1, 2[) = [-1, 3[$.
:::
