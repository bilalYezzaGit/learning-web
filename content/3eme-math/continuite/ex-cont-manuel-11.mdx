---
type: exercise
title: Image d'intervalles avec racine (11)
difficulty: 2
timeMinutes: 15
category: approfondissement
tags:
  - continuite
  - manuel
  - image-intervalle
  - racine
source: Manuel scolaire T1, Exercice 11, p.36
---

:::enonce
Soit $f : x \mapsto \sqrt{x + 3} - 2$.

1. Determiner l'ensemble de definition de $f$.
2. Tracer la courbe representative $C$ dans un repere orthonorme.
3. Quelles sont les images par $f$ des intervalles $I = [2, 3]$, $J = [0, 5]$ et $K = ]-1, +\infty[$ ?
4. Determiner l'ensemble des antecedents par $f$ des reels de l'intervalle $[-1, +\infty[$.
:::

:::solution
1. $f$ est definie lorsque $x + 3 \geq 0$, soit $x \geq -3$. Donc $D_f = [-3, +\infty[$.

2. $f$ est la courbe de $\sqrt{x}$ translatee de $3$ vers la gauche et $2$ vers le bas. $f(-3) = -2$, $f(0) = \sqrt{3} - 2 \approx -0{,}27$, $f(1) = 0$, $f(6) = 1$.

```typst
#import "@preview/cetz:0.3.4": canvas, draw
#import "@preview/cetz-plot:0.1.1": plot

#canvas({
  plot.plot(
    size: (8, 6),
    x-label: $x$,
    y-label: $y$,
    x-tick-step: 1,
    y-tick-step: 1,
    x-min: -3, x-max: 6,
    y-min: -2.5, y-max: 2,
    {
      plot.add(domain: (-2.99, 6), x => calc.sqrt(x + 3) - 2,
        style: (stroke: 2pt + blue))
    },
  )
})
```

3. $f$ est croissante sur $[-3, +\infty[$ (car $\sqrt{x}$ est croissante).
- $f(I) = f([2, 3])$ : $f(2) = \sqrt{5} - 2$, $f(3) = \sqrt{6} - 2$. Donc $f([2, 3]) = [\sqrt{5} - 2, \sqrt{6} - 2]$.
- $f(J) = f([0, 5])$ : $f(0) = \sqrt{3} - 2$, $f(5) = \sqrt{8} - 2 = 2\sqrt{2} - 2$. Donc $f([0, 5]) = [\sqrt{3} - 2, 2\sqrt{2} - 2]$.
- $f(K) = f(]-1, +\infty[)$ : $f(-1) = \sqrt{2} - 2$ (non atteint) et $\lim_{x \to +\infty} f(x) = +\infty$. Donc $f(]-1, +\infty[) = ]\sqrt{2} - 2, +\infty[$.

4. $f(x) \geq -1$ : $\sqrt{x + 3} - 2 \geq -1 \Rightarrow \sqrt{x + 3} \geq 1 \Rightarrow x + 3 \geq 1 \Rightarrow x \geq -2$. L'ensemble des antecedents est $[-2, +\infty[$.
:::

:::methode
La fonction $x \mapsto \sqrt{x}$ est croissante, donc les images d'intervalles se calculent directement a partir des bornes.
:::
